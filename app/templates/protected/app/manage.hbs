<div local-class="app-manage">
    <div local-class="list">
      {{!-- {{#each @model.apps as |app|}}
        <LinkTo local-class="link" @route="protected.admin-apps.id" @model={{app.id}}>
          {{app.name}}
        </LinkTo>
      {{/each}} --}}
      
      <LinkTo @route="protected.app.manage.api">
        API
      </LinkTo>
    </div>
  {{outlet}}
</div>